{
  "name": "sistema-tripoliven",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema Logístico y de Gestión Logística Tripoliven",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm run electron:dev",
    "start": "concurrently \"npm run start:server\" \"npm run start:client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
